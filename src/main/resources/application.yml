spring:
  profiles:
    active: dev

---
spring:
  config:
    activate:
      on-profile: dev
  liquibase:
    change-log: classpath:/db/changelog/dbchangelog.xml
    enabled: true
  datasource:
    platform: postgres
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://172.25.183.156:5432/postgres
    username: postgres
    password: mysecretpassword
  jpa:
    database: POSTGRESQL
    show-sql: true
---
spring:
  config:
    activate:
      on-profile:
       test
  datasource:
    platform: h2
    url: jdbc:h2:mem:mydb
    username: sa
    password:
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console.enabled: true

---

logging:
  file:
    name: liquorlog1.log


